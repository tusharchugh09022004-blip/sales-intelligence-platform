version: '3.8'

services:
  sales-analytics-app:
    build: .
    container_name: sales_analytics_platform
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
    restart: unless-stopped
